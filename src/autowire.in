#!/bin/bash
_launch_dir="$(dirname "$(readlink -f "$0")")"
_src_dir="$_launch_dir/../../src"
_main_js="$_src_dir/main.js"
if [ -f "$_main_js" ]; then
    exec @GJS@ -I "$_src_dir" "$_main_js" "$@"
else
    exec @GJS@ -I @PKGDATADIR@ @PKGDATADIR@/main.js "$@"
fi
